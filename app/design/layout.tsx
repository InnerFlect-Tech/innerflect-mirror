import './design.css';

/**
 * The design system's own surfaces. Deliberately outside the product shell: this
 * is where the visual language is inspected, not where the company is operated.
 */
export default function DesignLayout({ children }: { children: React.ReactNode }) {
  return children;
}
