export type { WorldDomain } from '@/lib/model/domain';

import type { WorldDomain } from '@/lib/model/domain';

export type DomainVisual = {
  domain: WorldDomain;
  position: [number, number, number];
  scale: number;
};
